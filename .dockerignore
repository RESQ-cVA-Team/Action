# Ignore everything by default
*

# Re-include root files we need
!requirements.txt
!Dockerfile
!README.md
!LICENSE

# Include entire src tree (recursively) so that nested YAML (shared/SSOT/*.yml) are not dropped
!src/
!src/**
!rasa_sdk_plugins/
!rasa_sdk_plugins/**

# Still ignore Python cache and editor artifacts (must come AFTER the re-include so they win)
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.DS_Store
.venv/
venv/
.idea/
.vscode/